import { Viewport } from "@timvir/blocks";

# Group

The `<Group>` component is used to group multiple images. The images are shown in a grid,
which can be regular (each image has equal size), or irregular (images have different sizes).

<Viewport src="/docs/components/Group/samples/basic" />
