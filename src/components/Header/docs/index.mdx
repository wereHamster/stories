import { Viewport } from "@timvir/blocks";

# Header

The `<Header>` component is used at the very top of a story page.

It displays:

 - the cover image
 - story title and subtitle
 - author and other metadata

<Viewport src="/docs/components/Header/samples/basic" />
