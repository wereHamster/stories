import { Viewport } from "@timvir/blocks";

# Content

Container for the story content. Is a CSS grid into which the story elements are placed.

<Viewport src="/docs/components/Content/samples/basic" />
